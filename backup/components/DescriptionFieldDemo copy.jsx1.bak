import React, { useState } from 'react'
import { DescriptionField } from './FormFields'

const DescriptionFieldDemo = () => {
  const [simpleDescription, setSimpleDescription] = useState('')
  const [structuredDescription, setStructuredDescription] = useState('')

  return (
    <div className="max-w-4xl mx-auto p-6 space-y-8">
      <h1 className="text-2xl font-bold text-gray-800 mb-6">
        Demo DescriptionField Component
      </h1>

      {/* Variant Simple */}
      <div className="bg-white p-6 rounded-lg shadow-md">
        <h2 className="text-lg font-semibold text-gray-700 mb-4">
          Variant Simple - Item dan Ringkasan Penerang
        </h2>
        <p className="text-sm text-gray-600 mb-4">
          Variant ini sesuai untuk penerangan ringkas dengan item-item yang mudah.
        </p>
        
        <DescriptionField
          value={simpleDescription}
          onChange={(e) => setSimpleDescription(e.target.value)}
          label="Penerangan Ringkas"
          placeholder="Masukkan penerangan ringkas..."
          variant="simple"
          rows={4}
        />

        <div className="mt-4 p-3 bg-blue-50 rounded-md">
          <h3 className="font-semibold text-blue-800 mb-2">Contoh Input:</h3>
          <pre className="text-sm text-blue-700 whitespace-pre-wrap">
{`**Perkhidmatan Utama**
Penyelenggaraan komputer
Pemasangan perisian
Sokongan teknikal

**Perkhidmatan Tambahan**
Konsultasi IT
Latihan pengguna`}
          </pre>
        </div>
      </div>

      {/* Variant Structured */}
      <div className="bg-white p-6 rounded-lg shadow-md">
        <h2 className="text-lg font-semibold text-gray-700 mb-4">
          Variant Structured - Tajuk dan Item
        </h2>
        <p className="text-sm text-gray-600 mb-4">
          Variant ini sesuai untuk penerangan yang terstruktur dengan tajuk dan item-item di bawahnya.
        </p>
        
        <DescriptionField
          value={structuredDescription}
          onChange={(e) => setStructuredDescription(e.target.value)}
          label="Penerangan Terstruktur"
          placeholder="Masukkan penerangan terstruktur..."
          variant="structured"
          rows={6}
        />

        <div className="mt-4 p-3 bg-green-50 rounded-md">
          <h3 className="font-semibold text-green-800 mb-2">Contoh Input:</h3>
          <pre className="text-sm text-green-700 whitespace-pre-wrap">
{`**Spesifikasi Teknikal**
Processor: Intel Core i7
RAM: 16GB DDR4
Storage: 512GB SSD
Graphics: NVIDIA RTX 3060

**Perisian Termasuk**
Windows 11 Pro
Microsoft Office 365
Adobe Creative Suite
Antivirus Premium

**Warranty & Support**
3 tahun warranty
24/7 technical support
On-site service`}
          </pre>
        </div>
      </div>

      {/* Contoh Penggunaan dalam Form */}
      <div className="bg-white p-6 rounded-lg shadow-md">
        <h2 className="text-lg font-semibold text-gray-700 mb-4">
          Contoh Penggunaan dalam Form
        </h2>
        
        <form className="space-y-4">
          <div>
            <label className="block text-sm font-medium text-gray-700 mb-2">
              Nama Produk
            </label>
            <input
              type="text"
              className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Masukkan nama produk..."
            />
          </div>

          <DescriptionField
            value={simpleDescription}
            onChange={(e) => setSimpleDescription(e.target.value)}
            label="Penerangan Produk"
            placeholder="Masukkan penerangan produk..."
            variant="simple"
            rows={3}
          />

          <DescriptionField
            value={structuredDescription}
            onChange={(e) => setStructuredDescription(e.target.value)}
            label="Spesifikasi Terperinci"
            placeholder="Masukkan spesifikasi terperinci..."
            variant="structured"
            rows={5}
          />

          <button
            type="submit"
            className="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition-colors"
          >
            Simpan Produk
          </button>
        </form>
      </div>
    </div>
  )
}

export default DescriptionFieldDemo
